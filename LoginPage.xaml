<Page x:Class="WPFApp_Navigation.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="LoginPage">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Email:" Margin="0,5,0,0"/>
            <TextBox Name="EmailTextBox" Width="200" Height="30" Margin="0,0,0,10"/>

            <TextBlock Text="Password:"/>
            <PasswordBox Name="PasswordBox" Width="200" Height="30" Margin="0,0,0,5"/>

            <TextBlock Text="Password forgotten?" Foreground="Blue" Cursor="Hand"
                       MouseLeftButtonUp="ForgotPassword_Click"
                       HorizontalAlignment="Left" Margin="0,0,0,10"/>

            <Button Content="Log In" Width="100" Click="Login_Click"/>
        </StackPanel>

        <TextBlock Text="Go back" Foreground="Blue" Cursor="Hand"
                   HorizontalAlignment="Left" VerticalAlignment="Top"
                   Margin="10" MouseLeftButtonUp="Back_Click"/>
    </Grid>
</Page>
