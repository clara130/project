<Page x:Class="WPFApp_Navigation.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="DashboardPage">
    <Grid>
        <Grid Margin="20" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonUp="Email_Click">
                <Ellipse Width="32" Height="32" Margin="0,0,5,0">
                    <Ellipse.Fill>
                        <ImageBrush x:Name="ProfileImageBrush" Stretch="UniformToFill" />
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock x:Name="EmailTextBlock" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="LastCall" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="Because life doesn't come with automatic reminders" FontStyle="Italic" FontSize="12" HorizontalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="Logout" Foreground="Red" FontWeight="Bold"
                       VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Column="2" Cursor="Hand"
                       MouseLeftButtonUp="Logout_Click"/>
        </Grid>

   
        <Grid Margin="20,80,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border BorderBrush="Gray" BorderThickness="1" Padding="20" Grid.Column="0">
                <StackPanel x:Name="CategoryStackPanel">
                    <TextBlock Text="Categories" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Button Content="Personal" Tag="Personal" Click="Category_Click" Margin="0,5" Height="35"/>
                    <Button Content="House" Tag="House" Click="Category_Click" Margin="0,5" Height="35"/>
                    <Button Content="Car" Tag="Car" Click="Category_Click" Margin="0,5" Height="35"/>
                    <Button Content="Education" Tag="Education" Click="Category_Click" Margin="0,5" Height="35"/>
                </StackPanel>
            </Border>

            <Border BorderBrush="Gray" BorderThickness="1" Padding="20" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="FUTURE DEADLINES" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ItemsControl x:Name="DeadlineList"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
